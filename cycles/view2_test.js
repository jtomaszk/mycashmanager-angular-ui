'use strict';

describe('myApp.cycles module', function() {

  beforeEach(module('myApp.cycles'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('CyclesCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});