'use strict';

describe('myApp.category module', function() {

  beforeEach(module('myApp.category'));

  describe('category controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('CategoryCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});